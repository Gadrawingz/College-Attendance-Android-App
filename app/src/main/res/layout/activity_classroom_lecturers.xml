<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="0dp"
    tools:context=".classroom.ClassroomLecturers"
    android:orientation="vertical">


    <TextView
        android:layout_height="50dp"
        android:layout_width="match_parent"
        android:layout_marginBottom="15dp"
        android:id="@+id/mainTitle"
        android:layout_marginTop="0dp"
        android:textSize="24dp"
        android:drawableLeft="@drawable/backarrow"
        android:drawableTint="@color/white"
        android:textStyle="bold"
        android:background="@drawable/lecturercourses"
        android:textColor="@color/white"
        android:padding="10dp"
        android:text="Lecturer's classes"
        android:gravity="center_horizontal"/>

    <ListView
        android:id="@+id/listViewClassGroup"
        android:layout_width="match_parent"
        android:background="@drawable/border_blue1"
        android:layout_height="wrap_content"/>

    <ProgressBar android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_gravity="center_horizontal"
        android:id="@+id/loadingProgBar"/>

</LinearLayout>